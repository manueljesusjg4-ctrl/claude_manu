# Configuración del servidor (este archivo se incluye a propósito para que la app arranque sin pasos extra;
# al ser una app de uso interno con SQLite local no contiene secretos de producción).
DATABASE_URL="file:./gestion.db"
JWT_SECRET="cambia-este-secreto-en-tu-ordenador-si-quieres"
PORT=3001
